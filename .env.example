# ===========================================
# ZORUNLU - NextAuth ve veritabanı
# ===========================================

# MongoDB bağlantı URI (Atlas veya local)
# Örnek: mongodb+srv://kullanici:sifre@cluster.xxxxx.mongodb.net/ticketing?retryWrites=true&w=majority
MONGO=

# NextAuth güvenlik anahtarı (JWT imzalama için zorunlu - /api/v1 auth da bunu kullanır)
# Üretmek için: openssl rand -base64 32
NEXTAUTH_SECRET=
# Opsiyonel: Sadece v1 API JWT için farklı key isterseniz
# JWT_SECRET=
# JWT_COOKIE_NAME=auth-token
# JWT_ACCESS_EXPIRY=15m

# Uygulama URL'i (local: http://localhost:3000)
NEXTAUTH_URL=http://localhost:3000

# ===========================================
# OPSIYONEL - OAuth (GitHub / Google)
# Kullanmayacaksanız boş bırakabilirsiniz.
# ===========================================

# GitHub OAuth: https://github.com/settings/developers -> New OAuth App
GITHUB_ID=
GITHUB_SECRET=

# Google OAuth: https://console.cloud.google.com/apis/credentials
GOOGLE_ID=
GOOGLE_SECRET=

# Admin e-posta (Google ile girişte bu e-posta "admin" rolü alır)
ADMIN=
